<Page x:Class="EF_Core.Pages.AddUserGroup"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:EF_Core.Pages"
      xmlns:services="clr-namespace:EF_Core.Models.Service"
      xmlns:validations="clr-namespace:EF_Core.Rules.Validations"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White"
      Title="AddUserGroup">

    <Page.Resources>
        <services:GroupService x:Key="group_list"/>
        <services:InteresGroupUserService x:Key="interesgr"/>
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="6*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="1" x:Name="User_Info">
            <Label>Пользователь:</Label>
            <TextBlock Text="{Binding Name}"/>
        </StackPanel>
        
        <Grid  Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" x:Name="Info">
                <Label>Дата вступления</Label>
                <StackPanel>
                    <DatePicker x:Name="Date" materialDesign:HintAssist.Hint="Дата вступления" Style="{StaticResource FormDatePicker}">
                        <DatePicker.SelectedDate>
                            <Binding Path="JoinedAt" UpdateSourceTrigger="PropertyChanged" >
                                <Binding.ValidationRules>
                                    <validations:Date/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DatePicker.SelectedDate>
                    </DatePicker>
                    <ItemsControl ItemsSource="{Binding ElementName=Date,Path=(Validation.Errors)}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ErrorContent}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <StackPanel>
                    <Label>Модератор</Label>
                    <CheckBox IsChecked="{Binding IsModerator}"/>
                </StackPanel>
                
            </StackPanel>
            <StackPanel  x:Name="List_group">
                <Label>Группы в которые можно в ступить</Label>
                <ListView ItemsSource="{Binding Source={StaticResource group_list},Path=Groups,UpdateSourceTrigger=PropertyChanged}"
                          SelectedItem="{Binding Group,UpdateSourceTrigger=PropertyChanged}"
                          >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding Title}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>


        </Grid>

        <StackPanel Grid.Row="2" Grid.Column="1" Style="{StaticResource Button_stack}">
            <Button Content="Добавить" Click="SelectGroup"/>
            <Separator Width="10" Visibility="Hidden"/>
            <Button Content="Назад" Click="Go_Back"/>
        </StackPanel>
    </Grid>
</Page>
