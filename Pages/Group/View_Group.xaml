<Page x:Class="EF_Core.Pages.Group.View_Group"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EF_Core.Pages.Group"
      xmlns:validations="clr-namespace:EF_Core.Rules.Validations"
      xmlns:services="clr-namespace:EF_Core.Models.Service"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White"
      Title="View_Group">

    <Page.Resources>
        <services:GroupService x:Key="group_Service"/>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="6*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <ListView ItemsSource="{Binding Source={StaticResource group_Service},Path=Groups,UpdateSourceTrigger=PropertyChanged}"
              SelectedItem="{Binding selected,UpdateSourceTrigger=PropertyChanged}"
              MouseDoubleClick="Edit"
              >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                    <Run Text="{Binding Title}"/>
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Grid.Column="1" Name="Add_group">
                <Label>Название группы</Label>
                <TextBox  Name="Title">
                    <TextBox.Text>
                        <Binding Path="Title" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validations:Empty/>
                                <validations:Group/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=Title,Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Label>Описание группы</Label>
                <TextBox x:Name="Description">
                    <TextBox.Text>
                        <Binding Path="Description" UpdateSourceTrigger="PropertyChanged" >
                            <Binding.ValidationRules>
                                <validations:Empty/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl ItemsSource="{Binding ElementName=Description,Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Grid.Column="1" Style="{StaticResource Button_stack}">
            <Button Content="Добавить" Click="Add_Group"/>
            <Separator Width="10" Visibility="Hidden"/>
            <Button Content="Удалить" Click="Delete_group"/>
            <Separator Width="10" Visibility="Hidden"/>
            <Button Content="Назад" Click="Go_back"/>
        </StackPanel>
    </Grid>
</Page>
